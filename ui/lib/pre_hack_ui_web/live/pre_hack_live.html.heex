<svg viewBox="0 0 100 100" class="w-full">
<%= for {{wave, color}, index} <- Enum.with_index(@waves) |> Enum.reverse() do %>
    <circle cx={50} cy={50} r={(((100/200)*(index+1))/2)-0.25} fill={"#"<>color}></circle>
<% end %>
</svg>